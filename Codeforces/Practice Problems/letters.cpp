#include <iostream>
#include <vector>
using namespace std;

int main () {
    string words;
    getline(cin, words);
    cout << words[0];
    if (words.size()==2)
    {
        cout << 0;
    }
    else // starts from one and is an odd number
    {

    }
}